<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET: serverga so'rov yuborish uchun (release build'da ba'zan unutiladi) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Kamera orqali rasm olish uchun (image_picker) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="Malumotnoma generatori"
        android:name="${applicationName}"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- iOS uchun metadata Flutter'ga kerak, Android'da zararsiz -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Android 9+ cleartext HTTP ni bloklaydi. Production HTTPS ishlatadi —
         bu config FAQAT lokal test uchun (10.0.2.2) ruxsat beradi.
         network_security_config.xml faylini res/xml/ ichiga qo'ying
         (mobile/README.md — "Lokal test" bo'limiga qarang). -->
    <queries>
        <!-- PDF'ni tashqi ilovada ochish (open_filex) uchun intent so'rovlari -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/pdf" />
        </intent>
    </queries>
</manifest>
