<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET: serverga so'rov yuborish uchun (release build'da ba'zan unutiladi) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Kamera orqali rasm olish uchun (image_picker) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="Malumotnoma generatori"
        android:name="${applicationName}"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher">

        <!--
          AdMob APP ID. Hozircha GOOGLE test ID — daromad bermaydi, lekin
          reklama test rejimida korinadi. Real daromad uchun:
          1. https://apps.admob.com da ilova royxatdan otkazing
          2. Shu yerga oz ca-app-pub-XXXX~YYYY ID ingizni yozing
          3. core/ads.dart dagi unit ID larni ham almastiring
          DIQQAT: notogri ID bilan chiqarilgan APK AdMob tomonidan bloklanishi mumkin.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- iOS uchun metadata Flutter'ga kerak, Android'da zararsiz -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Android 9+ cleartext HTTP ni bloklaydi.
         Lokal test manzillari (emulyator, Wi-Fi IP) uchun istisno berilgan.
         Production HTTPS ishlatadi - boshqa HTTP domen kerak bo'lsa shu yerga qoshing. -->
    <queries>
        <!-- PDF'ni tashqi ilovada ochish (open_filex) uchun intent so'rovlari -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/pdf" />
        </intent>
    </queries>
</manifest>
